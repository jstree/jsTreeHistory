<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd" >
<sqlMap namespace="compn">
	<typeAlias alias="compnVO"
		type="standard.mvc.component.business.baroboard.core.manage.setting.compn.vo.CompnVO" />
	<sql id="getMetaField">
		/* getChildNode */
		SELECT A.c_id
		, A.c_parentid
		, A.c_position
		, A.c_left
		, A.c_right
		, A.c_level
		, A.c_title
		, A.c_type
		/* user filed add
		point*/
	</sql>
	<sql id="getInlineChildCount">
		, ( SELECT CASE
		WHEN COUNT(*) &gt; 0 THEN 'InChild'
		ELSE
		'NoChild'
		END AS YesNo
		/* inline table from filed add point*/
	</sql>
	<sql id="getInlineChildCountAfterFrom">
		WHERE B.C_PARENTID = A.C_ID
		) AS childcount
		/* table from
		filed add point*/
	</sql>
	<sql id="getAfterFrom">
		WHERE A.C_PARENTID = #c_id#
		ORDER BY A.C_POSITION ASC
	</sql>

	<select id="getChildNode" parameterClass="compnVO" resultClass="compnVO">
		<dynamic>
			<include refid="getMetaField" />
			/* user filed */
			, C_COMPN_TITLE AS compnTitle
			, C_COMPN_VERSION AS compnVersion
			, C_COMPN_AUTHOR AS compnAuthor
			, C_COMPN_PATH AS compnPath
			<include refid="getInlineChildCount" />
			/* inline table from filed*/
			FROM T_CORE_SET_ADD_COMPN B
			<include refid="getInlineChildCountAfterFrom" />
			/* table from filed*/
			FROM T_CORE_SET_ADD_COMPN A
			<include refid="getAfterFrom" />
		</dynamic>
	</select>

</sqlMap>